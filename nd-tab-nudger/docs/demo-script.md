# Demo Script (draft)

1. Open a handful of tabs: a work doc, a reference article, and Reddit —
   left running for a few minutes so staleness has something to show.
2. Open a New Tab: show the ranked nudge cards (work doc surfaces above
   the reference tab because of the RRF fusion — call out staleness +
   semantic closeness explicitly).
3. Click "Take me there" on a card — show it jumps to the actual tab.
4. Switch to the Reddit tab — show the corner overlay card appear with
   the witty one-liner, referencing the same actionable tab.
5. Dismiss the overlay — show it doesn't reappear this session.
6. Briefly show the VectorAI DB collections (`tabs`, `labels`) via a
   quick curl/console, tying back to storage + classification +
   hybrid fusion (the three required behaviors).

TODO: fill in the exact tabs/URLs used once seed data is finalized.
